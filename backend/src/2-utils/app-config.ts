class AppConfig {

    // initialise database 
    public host = "localhost"
    public user = "root"
    public password = ""
    public database = "mesima3"

    // initialise server
    public port = 3001
    
}

const appConfig = new AppConfig()

export default appConfig